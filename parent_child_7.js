render () {
let { addressInfo, currentUserPos } = this.props;
let { addHouseNumber,
    addStreetName,
    addStreetDirection,
    addCity,
    addState,
    addZip,
    addLat,
    addLon } = addressInfo;
return ( ... );
}
